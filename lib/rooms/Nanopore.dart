import 'package:flutter/material.dart';

class Nanopore extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Scaffold(
      appBar: new AppBar(backgroundColor: Colors.purple, title: new Text("Nanopore Lab"),),
    );
  }
}
