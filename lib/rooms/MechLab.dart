import 'package:flutter/material.dart';

class MechLab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Scaffold(
      appBar: new AppBar(backgroundColor: Colors.blue, title: new Text("Mechatronics Lab"),),
    );
  }
}
