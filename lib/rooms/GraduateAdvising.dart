import 'package:flutter/material.dart';

class GraduateAdvising extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Scaffold(
      appBar: new AppBar(backgroundColor: Colors.deepPurple, title: new Text("Graduate Advising Office"),),
    );
  }
}
