import 'package:flutter/material.dart';

class CompGenLab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Scaffold(
      appBar: new AppBar(backgroundColor: Colors.purple, title: new Text("Computational Genomics Lab"),),
    );
  }
}
