import 'package:flutter/material.dart';

class AOL extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Scaffold(
      appBar: new AppBar(backgroundColor: Colors.indigo, title: new Text("Applied Optics Lab"),),
    );
  }
}