import 'package:flutter/material.dart';

class EE101 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Scaffold(
      appBar: new AppBar(backgroundColor: Colors.indigo, title: new Text("Eletrical Engineering 101 Lab"),),
    );
  }
}
